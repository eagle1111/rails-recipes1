<h1> 分组管理 </h1>

<p class="text-right">
<%= link_to "新增分组", new_admin_group_path, :class => "btn btn-primary" %>
</p>

<table class="table">
<tr>
  <th>分组名称</th>

</tr>
<% @groups.each do |group| %>
  <tr>
    <td><%=  group.name %></td>

  </tr>
<% end %>
</table>
