<h1><%= @event.name %></h1>
<%= @event.created_at.strftime("%Y/%m/%d %I:%M %p") %>

<%= simple_format @event.description %>

<p><%= link_to "新增日志", new_event_registration_path(@event), :class => "btn btn-primary" %></p>
